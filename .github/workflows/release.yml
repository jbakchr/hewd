name: Release hewd binaries

on:
  push:
    tags:
      - "v*"
  workflow_dispatch: {}

permissions:
  contents: write # required for uploading release assets

jobs:
  build:
    name: Build for ${{ matrix.goos }} / ${{ matrix.goarch }}
    runs-on: ubuntu-latest

    strategy:
      matrix:
        goos: [linux, darwin, windows]
        goarch: [amd64, arm64]

    env:
      CGO_ENABLED: 0

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.22"

      - name: Extract version from tag
        id: version
        run: |
          echo "GITHUB_REF=$GITHUB_REF"
          VERSION="${GITHUB_REF#refs/tags/}"
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Build binary
        run: |
          mkdir -p dist

          OUTPUT="hewd"
          if [ "${{ matrix.goos }}" = "windows" ]; then
            OUTPUT="hewd.exe"
          fi

          echo "Building ${OUTPUT} for ${{ matrix.goos }} / ${{ matrix.goarch }}"

          GOOS=${{ matrix.goos }} GOARCH=${{ matrix.goarch }} \
          go build \
            -trimpath \
            -ldflags="-s -w -X github.com/jbakchr/hewd/internal/version.Version=${VERSION}" \
            -o dist/${OUTPUT} \
            ./cmd/hewd

      - name: Package archive
        run: |
          cd dist
          NAME="hewd_${VERSION}_${{ matrix.goos }}_${{ matrix.goarch }}"

          if [ "${{ matrix.goos }}" = "windows" ]; then
            zip ${NAME}.zip hewd.exe
            echo "Created ${NAME}.zip"
          else
            tar -czf ${NAME}.tar.gz hewd
            echo "Created ${NAME}.tar.gz"
          fi

      - name: Generate checksums
        run: |
          cd dist
          sha256sum * > SHA256SUMS

      - name: Upload release assets
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
